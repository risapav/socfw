# Examples

Reference projects showing pack-aware socfw usage. Each example is a minimal, self-contained project that can be used as a starting point.

Use `socfw init` to scaffold a new project from a template.
