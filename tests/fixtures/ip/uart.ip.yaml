version: 2
kind: ip

ip:
  name: uart
  module: uart
  category: peripheral

origin:
  kind: source

integration:
  needs_bus: true
  generate_registers: true
  instantiate_directly: true
  dependency_only: false

reset:
  port: rst_n
  active_high: false
  bypass_sync: false

clocking:
  primary_input_port: clk

artifacts:
  synthesis:
    - uart.sv
